sudo: required
services:
  - docker

before_install:
  - docker build -t pierremilvue/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true USERNAME/react-test npm test

after_success:
  - docker built -t pierremilvue/multi-client ./client
  - docker built -t pierremilvue/multi-nginx ./nginx
  - docker built -t pierremilvue/multi-server ./server
  - docker built -t pierremilvue/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push pierremilvue/multi-client
  - docker push pierremilvue/multi-nginx
  - docker push pierremilvue/multi-server
  - docker push pierremilvue/multi-worker